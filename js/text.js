// text.js - 텍스트 처리 관련 함수들

function showScriptEditor(type) {
    if (type === 'manual') {
        alert('직접 대본 작성 에디터 (구현 예정)');
    } else if (type === 'ai') {
        alert('AI 대본 생성 (구현 예정)');
    }
}

function showNarrationEditor(type) {
    if (type === 'manual') {
        alert('직접 나레이션 작성 에디터 (구현 예정)');
    } else if (type === 'ai') {
        alert('AI 나레이션 생성 (구현 예정)');
    }
}